#1. Cài đặt môi trường ảo venv
python -m venv venv
#Gọi đến venv
.\venv\Scripts\Activate.ps1
#nâng cấp pip
python -m pip install --upgrade pip
#Tải file thư viện
pip install -r requirements.txt


#2. Chạy main.py để hiển thị model và các biểu đồ
#3. Chạy InpuInfo.py để nhập thông tin